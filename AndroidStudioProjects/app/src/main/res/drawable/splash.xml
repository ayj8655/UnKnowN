<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 배경 -->
    <item
        android:id="@+id/list_item_splash"
        android:top="10dp"
        android:bottom="10dp"
        android:left="10dp"
        android:right="10dp"
        />

    <!-- 로고 -->
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/unknown_splash"/>
    </item>
</layer-list>